# Cloud Export Configuration (Optional)
# Only needed if you want to use a custom cloud export endpoint
# By default, the app uses the built-in Next.js API route at /api/export

# NEXT_PUBLIC_EXPORT_API_URL=/api/export

# For production deployment, you can point this to a serverless function
# NEXT_PUBLIC_EXPORT_API_URL=https://your-domain.com/api/export
